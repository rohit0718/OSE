apiVersion: v1
kind: Pod
metadata:
  name: ose-test
  labels:
    app: ose-test
spec:
  schedulerName: ose
  containers:
  - name: ose-test-container
    image: gcr.io/google_containers/pause-amd64:3.0
    resources:
      limits:
        memory: "1024Mi"
        cpu: "2000m"
